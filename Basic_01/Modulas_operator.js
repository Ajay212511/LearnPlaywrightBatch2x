let a=1;
let b=3;
let c=a%b; // here % is an operator which is used to perform modulus operation on a and b and assign the result to c
console.log("value of c :"+c); // here % is an operator which is used to perform modulus operation on a and b and assign the result to c

a+=5; // here += is an operator which is used to perform addition assignment operation on a and 5 and assign the result to a
console.log("value of a :"+a); // here += is an operator which is used to perform addition assignment operation on a and 5 and assign the result to a   

a-=2; // here -= is an operator which is used to perform subtraction assignment operation on a and 2 and assign the result to a
console.log("value of a :"+a); // here -= is an operator which is used to perform subtraction assignment operation on a and 2 and assign the result to a    
a*=3; // here *= is an operator which is used to perform multiplication assignment operation on a and 3 and assign the result to a
console.log("value of a :"+a); // here *= is an operator which is used to perform multiplication assignment operation on a and 3 and assign the result to a     
a/=2; // here /= is an operator which is used to perform division assignment operation on a and 2 and assign the result to a

console.log("value of a :"+a); // here /= is an operator which is used to perform division assignment operation on a and 2 and assign the result to a    
a%=2;
console.log("value of a :"+a); // here %= is an operator which is used to perform modulus assignment operation on a and 2 and assign the result to a
a**=3;
console.log("value of a :"+a); // here **= is an operator which is used to perform exponentiation assignment operation on a and 3 and assign the result to a

