//comparision operator
let x=10;
let y=20;
console.log(x==y); // here == is an operator which is used to compare the value of x and y and return 
// true if they are equal and false if they are not equal
console.log(x!=y); // here != is an operator which is used to compare the value of x and y and return 
// true  if they are not equal and false if they are equal  

console.log(x===y); // here === is an operator which is used to compare the value of x and y and return
// true if they are equal and false if they are not equal
console.log(x!==y); // here !== is an operator which is used to compare the value of x and y and return
// true if they are not equal and false if they are equal
console.log(x>y); // here > is an operator which is used to compare the value of x and y and return
// true if x is greater than y and false if x is less than or equal to y
console.log(x<y); // here < is an operator which is used to compare the value of x and y and return 
// true if x is less than y and false if x is greater than or equal to y
console.log(x>=y); // here >= is an operator which is used to compare the value of x and y and return

// true if x is greater than or equal to y and false if x is less than y
console.log(x<=y); // here <= is an operator which is used to compare the value of x and y and return
// true if x is less than or equal to y and false if x is greater than y   

console.log(10>9); 