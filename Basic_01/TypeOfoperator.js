console.log(typeof "Hello"); // here typeof is an operator which is used to determine the type of a variable or a value and return it as a string
console.log(typeof 10);
console.log(typeof true);
console.log(typeof undefined);
console.log(typeof null); // here typeof is an operator which is used to determine the type of a variable or a value and return it as a string
console.log(typeof NaN); // here typeof is an operator which is used to determine the type of a variable or a value and return it as a string   
console.log(typeof {}); // here typeof is an operator which is used to determine the type of a variable or a value and return it as a string
console.log(typeof []); // here typeof is an operator which is used to determine the type of a variable or a value and return it as a string    
console.log(typeof function(){}); // here typeof is an operator which is used to determine the type of a variable or a value and return it as a string
console.log(typeof Symbol("id")); // here typeof is an operator which is used to determine the type of a variable or a value and return it as a string
console.log(typeof BigInt(123)); // here typeof is an operator which is used to determine the type of a variable or a value and return it as a str      
console.log(typeof 10n); // here typeof is an operator which is used to determine the type of a variable or a value and return it as a string   
console.log(typeof 3.14); // here typeof is an operator which is used to determine the type of a variable or a value and return it as a string

console.log (`${5*40}`);

